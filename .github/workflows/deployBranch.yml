name: Notify - Environment for development

on: 'pull_request'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:  
      - name: Fetch API Notify create pull_request 📦
        uses: JamesIves/fetch-api-data-action@v2
        with:
          endpoint: https://dash.velloware.com/api/notification
          configuration: '{ "method": "POST", "body": {"message": "✔ Create Environment 📣\n ambient for ${{ github.ref_name }}"} }'
